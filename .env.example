# Server-side Contentful credentials (set these in EdgeOne environment variables, NOT as VITE_ in production)
CONTENTFUL_SPACE_ID=your_space_id_here
CONTENTFUL_ACCESS_TOKEN=your_access_token_here
CONTENTFUL_ENVIRONMENT=master

# Client-side non-secret config
VITE_WHATSAPP_NUMBER=628123456789

# Note: For local development, you can also add VITE_ prefixed vars to .env for convenience,
# but move the real secret tokens to EdgeOne Project > Settings > Environment Variables before deploy.
# e.g. VITE_CONTENTFUL_SPACE_ID=your_space_id_here (local only)
